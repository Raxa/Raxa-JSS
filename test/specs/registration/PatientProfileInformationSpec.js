describe("Registration: Patient Profile Information", function() {

	/////////////////////////////////
	// UI Verifications
	/////////////////////////////////
	// Page navigation
	it("Can navigate back to previous page via BACK button", function() {
		/*expect(false).toBeTruthy();*/
	});

	it("Can navigate to Patient Communication Information page via CONTINUE button", function() {
		// All required fields must be present
		// All fields must have form validation run
	});

	it("Has focus on 'Patient Profile Information' in the navigation carousel", function() {
		// Patient Profile Information IS focused
		// Patient Communication Information is NOT focused
	});

	// User Input
	it("Shows patient registration number", function() {
		// TODO: Determine if registration # is a user inputted field? Or is it automatically generated? Ideally, automatically generated and displayed, to minimize input errors
	});

	it("Shows first name field", function() {

	});

	it("Shows last name field", function() {

	});

	it("Shows father/husband first name field", function() {

	});

	it("Shows father/husband last name field", function() {

	});

	it("Shows gender radio buttons", function() {

	});

	it("Shows date of birth inputs", function() {

	});

	it("Shows education radio buttons", function() {
		// TODO: better to have a dropdown?
	});

	it("Shows caste dropdown", function() {
		// Dropdown is present
		// Dropdown label is correct
		// Dropdown options are correct
	});

	it("Shows occupation dropdown", function() {
		// Dropdown is present
		// Dropdown label is correct
		// Dropdown options are correct
	});

	/////////////////////////////////
	// Functionality Verifications
	/////////////////////////////////
	it("Calculates current age according to Date of Birth input", function() {

	});

	it("Saves user input to HTML5 local storage on navigation to a different page", function() {

	});

});

